# Conference_-2
Модуль классификации ошибок времени выполнения с использованием методов NLP


Цель исследования: Автоматически categorize ошибки (Exception types) и предлагать возможные причины их возникновения, анализируя не только тип исключения, но и контекст его возникновения.

Суть: Модуль анализирует стектрейсы, логи и состояние переменных в момент ошибки. 
Текстовые данные (имена функций, переменных, значений) обрабатываются с помощью техник NLP (например, преобразование в Bag of Words или эмбеддинги). 
Модель машинного обучения (например, Gradient Boosting или нейросеть) обучается относить ошибку к определенному классу 
(напр., «Ошибка связана с работой с базой данных», «Ошибка валидации входных данных»).

Методы ИИ: Обработка естественного языка (NLP), классификационный ML.

Выход: Интеллектуальный анализатор логов, который помогает разработчику быстрее понять категорию и источник ошибки.
Конечно. Вот готовое техническое описание (Documentation/Onboarding), адаптированное специально для разработчика, который знает Python, но «плавает» в ML.

Я использовал понятные программисту аналогии, избегая сложной математики.

-----

## 📑 Документация к модулю: `AI Error Classifier`

### 1\. В двух словах: Что это?

Это «умная обертка» над стандартным механизмом логирования Python.

Обычный логгер пишет: `IndexError: list index out of range`.
Наш модуль пишет: **«Это Ошибка Логики (Logic\_Calculation\_Error), потому что ты пытался обратиться к 5-му элементу массива, а в нем всего 3 элемента».**

**Суть:** Мы заменяем ручной дебаг (чтение трейсбеков) на автоматическую классификацию причин сбоя.

-----

### 2\. Откуда данные? (Входной поток)

Главная фишка не в нейросети, а в том, **как мы собираем данные**.

Когда происходит `Exception`, стандартный Python показывает только стек вызовов. Мы идем глубже:

1.  **Перехват:** Используем `sys.exc_info()` внутри `except`.
2.  **Инспекция памяти:** Лезем в объект стека (`traceback`), берем последний фрейм (`tb_frame`) и вытаскиваем словарь локальных переменных (`f_locals`).
      * *Пример:* Если код упал, мы видим не только ошибку, но и то, что `user_id=None` или `retry_count=5`.
3.  **Склейка:** Текст ошибки + Имена функций в стеке + Значения переменных = **Единый текстовый документ**.

> **Для коллеги:** Мы буквально делаем `dump` состояния программы в момент смерти и скармливаем это модели.

-----

### 3\. Что под капотом? (ML для программиста)

Здесь нет магии и ChatGPT. Здесь работает классическое машинное обучение (Classical ML). Весь пайплайн состоит из двух шагов, понятных любому, кто работал с данными.

#### Шаг А: Превращаем текст в цифры (NLP)

Компьютер не умеет читать текст, он понимает только матрицы (массивы чисел).
Мы используем технику **TF-IDF (Term Frequency - Inverse Document Frequency)**.

  * **Как это работает:** Представь огромную Excel-таблицу, где колонки — это все уникальные слова, которые встречались в наших логах (`port`, `list`, `none`, `timeout`...), а строки — это конкретные ошибки.
  * Если слово `port` встречается в тексте ошибки, в ячейку ставится число (вес).
  * **Итог:** Текст ошибки превращается в вектор (одномерный массив): `[0.0, 0.5, 0.0, 0.9, ...]`.

#### Шаг Б: Классификация (Gradient Boosting)

Мы используем алгоритм **Gradient Boosting**.

  * **Аналогия:** Представь, что мы написали 1000 вложенных `if-else` условий.
      * `if "port" in text AND "5432" in text -> return "DatabaseError"`
      * `if "index" in text AND "len" in text -> return "LogicError"`
  * Вместо того чтобы писать эти `if` руками, мы "скармливаем" алгоритму тысячи примеров, и он **сам строит это дерево решений**.

-----

### 4\. Архитектура проекта (Кто за что отвечает)

Проект разбит на 3 независимых модуля, как в микросервисах:

1.  **`collector.py` (Сборщик / Логгер)**

      * *Роль:* ETL-скрипт.
      * *Что делает:* Ловит ошибку -\> Чистит регулярками (убирает адреса памяти `0x00f`, чтобы не шумели) -\> Сохраняет в CSV. Используется и для сбора датасета, и в рантайме.

2.  **`trainer.py` (Компилятор модели)**

      * *Роль:* Build-step.
      * *Что делает:* Берет CSV с примерами ошибок, строит матрицу слов (TF-IDF), обучает классификатор и сохраняет его в бинарный файл `error_classifier.pkl`.
      * *Зачем:* Запускается один раз (или в CI/CD), чтобы создать "мозги".

3.  **`predictor.py` (Рантайм)**

      * *Роль:* Production Interface.
      * *Что делает:* Загружает `pkl`-файл (десериализация). Принимает новую ошибку, прогоняет через модель за миллисекунды и выдает вердикт + совет на русском языке.

-----

### 5\. Рабочий процесс (Workflow)

1.  **Training (Обучение):**
    Мы запускаем генератор данных (`data_generator.py`), который специально "ломает" программу 1000 раз разными способами (валится БД, деление на ноль и т.д.). Модель смотрит на это и запоминает паттерны.

2.  **Inference (Боевое применение):**
    Ты вставляешь в свой код:

    ```python
    try:
        main_logic()
    except Exception:
        # Вместо print(e)
        smart_analyze(*sys.exc_info())
    ```

    Модель видит контекст, который ты не видел (переменные), и говорит: *"Чувак, у тебя ошибка валидации, потому что переменная `age` стала отрицательной"*.

### Резюме

Это не просто "поиск по тексту". Это система, которая учится на **состоянии переменных** в момент сбоя, чтобы различать ошибки, которые выглядят одинаково (например, `ValueError`), но имеют разные бизнес-причины.